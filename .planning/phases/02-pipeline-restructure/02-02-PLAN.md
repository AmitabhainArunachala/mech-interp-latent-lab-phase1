# Plan 02-02: Move Discovery/Archive Pipelines and Update Imports

## Goal

Move remaining pipelines and update all import paths in registry.py.

## Prerequisites

- Plan 02-01 complete (directory structure exists, canonical moved)

## Tasks

### Task 1: Move Discovery Pipelines (12 files)
- [ ] `c2_rv_measurement.py` → `discovery/`
- [ ] `behavioral_grounding.py` → `discovery/`
- [ ] `behavioral_grounding_batch.py` → `discovery/`
- [ ] `eigenstate_direction_finder.py` → `discovery/`
- [ ] `logit_lens_analysis.py` → `discovery/`
- [ ] `vproj_patching_analysis.py` → `discovery/`
- [ ] `mlp_vproj_combined_sufficiency_test.py` → `discovery/`
- [ ] `path_patching_mechanism.py` → `discovery/`
- [ ] `hysteresis.py` → `discovery/`
- [ ] `temporal_stability.py` → `discovery/`
- [ ] `kv_mechanism.py` → `discovery/`
- [ ] `layer_sweep.py` → `discovery/`

### Task 2: Move Archive Pipelines (35 files)
Move all remaining .py files to `archive/` including subdirectories.

### Task 3: Update registry.py Imports
- [ ] Update all import paths from `.foo` to `.canonical.foo` / `.discovery.foo` / `.archive.foo`
- [ ] Update `__init__.py` exports

### Task 4: Test Import
- [ ] Run `python -c "from src.pipelines.registry import get_registry; print(len(get_registry()))"`
- [ ] Verify count matches expected (41 experiments)

## Outputs

- All pipelines in correct directories
- registry.py updated with new import paths
- All imports working

## Success Criteria

- `python -c "from src.pipelines.registry import get_registry"` succeeds
- No import errors
