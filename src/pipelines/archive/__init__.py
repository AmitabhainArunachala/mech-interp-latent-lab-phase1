"""Archive pipelines - historical/superseded code, not for active use.

NOTE: These are imported lazily by registry.py to avoid circular import issues.
Do not add eager imports here - files have cross-dependencies that need fixing.
"""

# Lazy imports only - actual imports happen in registry.py
