# Plan 04-02: Reorganize Results Structure

## Objective
Move 137 result runs into three-tier structure matching pipelines/configs organization.

## Target Structure
```
results/
├── canonical/           # Paper figures (10 runs)
│   ├── rv_l27_causal_validation/
│   ├── confound_validation/
│   └── c2_measurement_suite/
├── discovery/           # Methodology (70 runs)
│   ├── behavioral_grounding/
│   ├── steering/
│   ├── path_patching/
│   └── phase0_validation/
├── archive/             # Historical (57 runs)
│   ├── failed/
│   └── superseded/
└── run_index.csv        # Updated index
```

## Steps

1. Create new directory structure
2. Move canonical runs (10)
3. Move discovery runs (70)
4. Move archive runs (24 + 33 failed = 57)
5. Clean up empty legacy directories
6. Update run_index.csv with new paths
7. Verify all runs accessible

## Verification
- Total runs before = Total runs after
- No broken symlinks
- run_index.csv updated
- Registry can still find results
