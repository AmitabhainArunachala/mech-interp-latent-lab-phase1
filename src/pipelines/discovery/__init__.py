"""Discovery pipelines - methodology tools for exploring new models.

NOTE: Imports happen lazily in registry.py to avoid circular dependencies.
"""

# Lazy imports only - actual imports happen in registry.py
